<div class="mx-auto max-w-5xl space-y-6"><div class="flex flex-col justify-between gap-4 sm:flex-row sm:items-end"><div><a href="{{ route('barbers.index') }}" wire:navigate class="text-sm font-semibold text-brand-600">← Volver a barberos</a><div class="mt-3 flex items-center gap-3"><h1 class="text-2xl font-extrabold text-slate-950">{{ $barber->display_name }}</h1><span class="rounded-full px-2.5 py-1 text-xs font-semibold {{ $barber->is_active?'bg-emerald-50 text-emerald-700':'bg-slate-200 text-slate-600' }}">{{ $barber->is_active?'Activo':'Inactivo' }}</span></div><p class="mt-1 text-sm text-slate-500">Usuario: {{ $barber->user->name }} · {{ $barber->user->email }}</p></div>@can('update',$barber)<div class="flex gap-2"><a href="{{ route('barbers.edit',$barber) }}" wire:navigate class="rounded-xl border border-slate-300 px-4 py-2.5 text-sm font-semibold">Editar</a><button wire:click="changeStatus" wire:confirm="¿Cambiar el estado de este barbero?" class="rounded-xl bg-red-600 px-4 py-2.5 text-sm font-bold text-white">{{ $barber->is_active?'Desactivar':'Activar' }}</button></div>@endcan</div>
@if(session('status'))<div class="rounded-xl border border-emerald-200 bg-emerald-50 px-4 py-3 text-sm text-emerald-800">{{ session('status') }}</div>@endif
<div class="grid gap-6 lg:grid-cols-3"><section class="rounded-2xl border border-slate-200 bg-white p-6 shadow-sm lg:col-span-2"><h2 class="font-bold text-slate-900">Horario laboral</h2><div class="mt-4 divide-y divide-slate-100">@foreach(App\Models\Barber::DAYS as $day=>$label)@php($schedule=$barber->work_schedule[$day])<div class="flex justify-between py-2.5 text-sm"><span class="font-medium text-slate-700">{{ $label }}</span><span class="text-slate-500">{{ $schedule['enabled'] ? $schedule['start'].' – '.$schedule['end'] : 'Descanso' }}</span></div>@endforeach</div></section><aside class="rounded-2xl border border-slate-200 bg-white p-6 shadow-sm"><h2 class="font-bold text-slate-900">Información</h2><dl class="mt-4 space-y-4"><div><dt class="text-xs font-bold uppercase text-slate-400">Teléfono</dt><dd class="mt-1 text-sm font-semibold">{{ $barber->phone }}</dd></div><div><dt class="text-xs font-bold uppercase text-slate-400">Comisión</dt><dd class="mt-1 text-lg font-bold">{{ number_format((float)$barber->default_commission_percentage,2) }}%</dd></div></dl></aside></div>
<section class="rounded-2xl border border-slate-200 bg-white p-6 shadow-sm"><h2 class="font-bold text-slate-900">Servicios que puede realizar</h2><div class="mt-4 flex flex-wrap gap-2">@foreach($barber->services as $service)<span class="rounded-full bg-brand-50 px-3 py-1.5 text-sm font-semibold text-brand-700">{{ $service->name }}</span>@endforeach</div></section></div>
