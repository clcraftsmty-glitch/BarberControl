<div class="mx-auto max-w-7xl space-y-6"><div class="flex flex-col justify-between gap-4 sm:flex-row sm:items-end"><div><p class="text-xs font-semibold uppercase tracking-[.16em] text-brand-600">Equipo</p><h1 class="mt-1 text-2xl font-extrabold text-slate-950">Barberos</h1><p class="mt-1 text-sm text-slate-500">Administra perfiles, horarios y capacidades.</p></div>@can('create', App\Models\Barber::class)<a href="{{ route('barbers.create') }}" wire:navigate class="rounded-xl bg-brand-600 px-4 py-2.5 text-center text-sm font-bold text-white">+ Nuevo barbero</a>@endcan</div>
@if(session('status'))<div class="rounded-xl border border-emerald-200 bg-emerald-50 px-4 py-3 text-sm text-emerald-800">{{ session('status') }}</div>@endif
<div class="rounded-2xl border border-slate-200 bg-white shadow-sm"><div class="grid gap-3 border-b border-slate-200 p-4 sm:grid-cols-[1fr_180px]"><input type="search" wire:model.live.debounce.350ms="search" placeholder="Buscar por nombre, teléfono o correo..." class="rounded-xl border-slate-300 text-sm"><select wire:model.live="status" class="rounded-xl border-slate-300 text-sm"><option value="all">Todos</option><option value="active">Activos</option><option value="inactive">Inactivos</option></select></div><div class="overflow-x-auto"><table class="min-w-full divide-y divide-slate-200"><thead class="bg-slate-50"><tr><th class="px-5 py-3 text-left text-xs font-bold uppercase text-slate-500">Barbero</th><th class="px-5 py-3 text-left text-xs font-bold uppercase text-slate-500">Contacto</th><th class="px-5 py-3 text-left text-xs font-bold uppercase text-slate-500">Comisión</th><th class="px-5 py-3 text-left text-xs font-bold uppercase text-slate-500">Servicios</th><th class="px-5 py-3 text-left text-xs font-bold uppercase text-slate-500">Estado</th><th class="px-5 py-3 text-right text-xs font-bold uppercase text-slate-500">Acciones</th></tr></thead><tbody class="divide-y divide-slate-100">@forelse($barbers as $barber)<tr><td class="px-5 py-4"><a href="{{ route('barbers.show',$barber) }}" wire:navigate class="font-semibold text-slate-900 hover:text-brand-600">{{ $barber->display_name }}</a><p class="text-xs text-slate-500">{{ $barber->user->name }}</p></td><td class="px-5 py-4 text-sm text-slate-700">{{ $barber->phone }}<p class="text-xs text-slate-500">{{ $barber->user->email }}</p></td><td class="px-5 py-4 text-sm">{{ number_format((float)$barber->default_commission_percentage,2) }}%</td><td class="px-5 py-4 text-sm">{{ $barber->services_count }}</td><td class="px-5 py-4"><span class="rounded-full px-2.5 py-1 text-xs font-semibold {{ $barber->is_active?'bg-emerald-50 text-emerald-700':'bg-slate-100 text-slate-600' }}">{{ $barber->is_active?'Activo':'Inactivo' }}</span></td><td class="whitespace-nowrap px-5 py-4 text-right"><a href="{{ route('barbers.show',$barber) }}" wire:navigate class="px-2 text-xs font-semibold">Ver</a>@can('update',$barber)<a href="{{ route('barbers.edit',$barber) }}" wire:navigate class="px-2 text-xs font-semibold text-brand-700">Editar</a><button wire:click="changeStatus({{ $barber->id }})" wire:confirm="¿Cambiar el estado de {{ $barber->display_name }}?" class="px-2 text-xs font-semibold text-red-600">{{ $barber->is_active?'Desactivar':'Activar' }}</button>@endcan</td></tr>@empty<tr><td colspan="6" class="px-5 py-14 text-center text-sm text-slate-500">No se encontraron barberos.</td></tr>@endforelse</tbody></table></div>@if($barbers->hasPages())<div class="border-t border-slate-200 px-5 py-4">{{ $barbers->links() }}</div>@endif</div></div>
