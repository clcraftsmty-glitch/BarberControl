<div class="mx-auto max-w-5xl space-y-6">
    <div class="flex flex-col justify-between gap-4 sm:flex-row sm:items-end"><div><a href="{{ route('services.index') }}" wire:navigate class="text-sm font-semibold text-brand-600">← Volver a servicios</a><div class="mt-3 flex items-center gap-3"><h1 class="text-2xl font-extrabold text-slate-950">{{ $service->name }}</h1><span class="rounded-full px-2.5 py-1 text-xs font-semibold {{ $service->is_active ? 'bg-emerald-50 text-emerald-700' : 'bg-slate-200 text-slate-600' }}">{{ $service->is_active ? 'Activo' : 'Inactivo' }}</span></div></div>@can('update', $service)<div class="flex gap-2"><a href="{{ route('services.edit', $service) }}" wire:navigate class="rounded-xl border border-slate-300 bg-white px-4 py-2.5 text-sm font-semibold text-slate-700">Editar</a><button wire:click="changeStatus" wire:confirm="¿Cambiar el estado de este servicio?" class="rounded-xl bg-red-600 px-4 py-2.5 text-sm font-bold text-white">{{ $service->is_active ? 'Desactivar' : 'Activar' }}</button></div>@endcan</div>
    @if (session('status')) <div class="rounded-xl border border-emerald-200 bg-emerald-50 px-4 py-3 text-sm font-medium text-emerald-800">{{ session('status') }}</div> @endif
    <section class="rounded-2xl border border-slate-200 bg-white p-6 shadow-sm"><h2 class="text-base font-bold text-slate-900">Descripción</h2><p class="mt-3 whitespace-pre-line text-sm leading-6 text-slate-600">{{ $service->description }}</p><dl class="mt-7 grid gap-5 border-t border-slate-200 pt-6 sm:grid-cols-3"><div><dt class="text-xs font-bold uppercase text-slate-400">Duración</dt><dd class="mt-1 text-lg font-bold text-slate-900">{{ $service->duration_minutes }} min</dd></div><div><dt class="text-xs font-bold uppercase text-slate-400">Precio</dt><dd class="mt-1 text-lg font-bold text-slate-900">${{ number_format((float) $service->price, 2) }}</dd></div><div><dt class="text-xs font-bold uppercase text-slate-400">Comisión</dt><dd class="mt-1 text-lg font-bold text-slate-900">{{ number_format((float) $service->commission_percentage, 2) }}%</dd></div></dl></section>
</div>
